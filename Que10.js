//Find surface area of sphere

let radius = Number(prompt("Enter radius:"));

//formula , pi=3.14
let surfaceArea = 4 * Math.PI * Math.pow(radius, 2);

console.log(`Surface area of sphere = ${surfaceArea}`);
alert(`Surface area of sphere = ${surfaceArea}`);